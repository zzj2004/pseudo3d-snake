# 美工升级方案(ART-Zcode.md,Zcode 出稿 → opencode 实现)

状态:**第二轮——老板提出三个主题化方向(笔墨纸砚/翠玉竹林/赛博 2077),Zcode 评估完毕,待老板选定主题后出该主题的逐元素设计稿**。第一轮的两条配色路线(翠玉丛林/暗夜霓虹)已被本轮主题方向吸收升级,见文末存档。
背景:老板授权放开"白底黑蛇"初始约定,做整体美工升级。本方案由 Zcode 出稿;实现分工归 opencode;老板只看最终界面效果验收。
硬约束(必须遵守):
1. 单文件零依赖:**禁止引入任何外部资源**(图片/字体/CSS 框架/图标库),全部效果用 canvas 2D 绘制
2. **只动绘制层**(`draw()`、`fillFruitShape()`、`drawWall()`、覆盖层与图鉴绘制);`step()/eat()/模式/存档` 等逻辑一律不碰
3. 兼容既有机制:虚影 globalAlpha 0.15 相乘、无敌闪烁 alpha 0.25、隐身 alpha 0.45、图鉴未点亮 alpha 0.35、`ctx.roundRect` 需保留 rect fallback
4. 性能:draw 挂在 60fps rAF 上,渐变与发光必须预渲染或逐节纯色,禁止每节每帧创建渐变/发光对象

---

## 第二轮:主题化方向(老板 2026-08-30 提案,三选一)

> 三个方向都可行,差异在工作量、风险和气质。Zcode 评估如下,推荐加粗。

### 主题一:「笔墨纸砚」(老板原创愿景,2.5D 伪 3D)——**Zcode 首推**

老板愿景原文:玩家控制笔,蛇身就是墨(笔画出的线),果实变成砚(砚中墨水颜色区分效果),纸就是背景(宣纸质感)。

**点睛设计(Zcode 补全)**:
- **纸**:宣纸底 `#f7f2e7`,纸纤维纹理(约 80 根极淡短弧线 `#e8e0cc`,一次性画进离屏 canvas 缓存,每帧贴图,零性能成本);**每层一纸**——第 1 层宣纸米黄、第 2 层竹纸青白 `#eef3ea`、第 3 层绢本淡褐 `#f2e8dc`,循环——层的可辨识度直接解决
- **蛇 = 一笔墨**:身体从"分节方块"升级为**连续笔触**——粗细从头 14px 渐变到尾 5px、墨色 `#1a1a1a`→`#555` 渐淡、外圈 2px 半透明墨晕 `rgba(26,26,26,0.15)`、尾段飞白(2–3 根细白线);转弯处胶囊连接天然圆滑。隐身果 = 笔洗过的淡墨(半透明现成)、彩虹果 = "墨分五色"浓淡渐变动画(比彩虹渐变更点题)
- **头 = 毛笔**:竹杆短节(两个色段拼立体)+ 黑色笔锋朝向移动方向;红色分叉吐信可保留(点睛)或删(更文人)
- **果 = 砚**:小砚台(椭圆深石身 `#3e4450` + 墨池),**墨池颜色 = 效果色**,色板全部换成国画颜料:朱砂/石绿/靛青/藤黄/紫矿/赭石/胭脂/青碧/泥金……区分度和文化味兼得
- **印章 UI**:得分、层号、倒计时全部画成**朱文印章**(红底 `#c0392b` 圆角方块 + 白字)——UI 统一且极点题;结算/通关界面 = 题跋卡片
- **2.5D**:全元素淡投影(offset 2px `rgba(0,0,0,0.12)`),笔杆双色拼立体;墙格 = 镇纸墨块(深墨方 + 石纹)
- **加分项**:爬层动画文案顺势改"翻页/换帖";通关界面可盖一个"通关"大印

- 工作量:三主题中最大(墨蛇笔触需调教),**建议分两期**:一期做 纸+墨蛇+砚果+印章 UI;二期补 2.5D 投影/立体细节
- 风险:中(飞白/墨晕容易画脏,需要 opencode 实现后试玩迭代 1–2 轮)
- 契合度:**与"伪三维"游戏名天作之合**——层 = 字帖的一页,爬层 = 翻帖;原创性和辨识度三主题最高

### 主题二:「翠玉竹林」(老板升级版:参考功夫熊猫竹叶青)

老板愿景:以我国竹叶青蛇为创作思路(通体翠绿、红眼、细长),果实变成不同颜色的竹子。

**点睛设计**:
- 竹叶青蛇特征:翠绿渐变身体(现有方案 A 基础上)+ **腹部白色侧线**(竹叶青标志)+ **三角头** + **红/橙色眼睛**(替代白眼,点睛)
- 背景:淡竹青 `#eef7ea`,远景竹竿竖条 `#d5e8d0`(离屏缓存)+ 几片竹叶剪影 `#c8e0c0`;层 = 竹林深处,越高层竹色越深(雾感递进,呼应层深度收益)
- 果 = 竹节:竖直圆角短柱 + 两道节环线,颜色 = 效果色(沿用主色表);弹珠方案的高光改为一道竖向高光条(圆柱感)
- 墙 = 竹桩/青石;印章 UI 不适用,得分文字用墨绿
- 工作量:三主题中**最小**(第一轮路线 A 的直接延伸,换背景+果子画法+蛇头细节);风险低,一次到位
- 契合度:高;但"绿蛇吃果子"辨识度弱于墨蛇

### 主题三:「赛博 2077」(老板升级版:机械电子风)

老板愿景:科技霓虹灯 + 电子模糊感背景,果实变成电池等电子物品,机械科技风蛇。

**点睛设计**:
- 底 `#0a0e1a`,电子网格 `#14203a`,全屏扫描线 overlay(1 次预渲染贴图);电子模糊感 = 少量漂浮光斑(2–3 个径向渐变预渲染圆,慢速漂移)
- 蛇 = 机械节段:金属渐变 `#37474f`→`#78909c` 圆角节 + 节间接缝线,头部**青色眼灯**;轮廓发光**必须预渲染**(用径向渐变光圈贴图,禁用 shadowBlur 逐节绘制——性能陷阱)
- 果 = 电池/能量芯:圆角矩形电池 + 正负极 + 电量条纹,颜色 = 能量色(暗底上现有高饱和主色全部适用且更亮)
- 墙 = 电磁屏障(黄黑/青黑警示斜纹);UI 文字全反白,虚影/图鉴/覆盖层全部按暗底重调——**这是三主题中适配面最大的**
- 工作量:中大;风险中(暗底对比度与发光性能需要控制)
- 契合度:中(科技感与"伪三维爬层"的文化叙事不冲突但也不互助);风格差异化最强

### Zcode 的推荐与理由

**首推主题一「笔墨纸砚」**:愿景最完整、最原创,文化叙事和"伪三维爬层"完美互文(层 = 字帖页),国画色板天然高区分度,印章 UI 一次统一所有 HUD。代价是工作量最大、需要 1–2 轮试玩迭代墨蛇笔触——但值得。
**求稳选主题二「竹林」**:改动最小、一次到位、观感雅致。
**主题三**适合作为未来的第二皮肤(模式×主题联动:有命=赛博、无尽=水墨,可以都要)。

> ~~选定后:Zcode 出该主题的逐元素设计稿~~ **老板已选:主题一「笔墨纸砚」(2026-08-30)。详细设计稿见下节,opencode 照稿实现。**

---

## ★ 定稿设计稿:「笔墨纸砚」逐元素规格(2026-08-30,Zcode 出稿 → opencode 实现)

> 实现版本号以 git log 为准(当前下一个是 v41),单独 commit。只动绘制层;逻辑零改动。
> 分期:本稿内容全部属于**一期**(含轻量投影与飞白);二期仅剩试玩后的细节打磨。

### 0. 色板总表(实现时集中为常量区)

| 用途 | 色值 |
| --- | --- |
| 书案(body 背景) | `#4a3f35` 胡桃木 |
| 画布边框 | 3px `#6d4c41` |
| 宣纸(z%3==1) | 底 `#f7f2e7` / 纤维 `#e8e0cc` |
| 竹纸(z%3==2) | 底 `#eef3ea` / 纤维 `#dbe5d6` |
| 绢本(z%3==0 且 z>1) | 底 `#f2e8dc` / 纤维 `#e4d6c2` |
| 浓墨(蛇头) | `#1a1a1a` |
| 淡墨(蛇尾端) | `#555555` |
| 墨晕 | `rgba(26,26,26,0.13)` |
| 飞白 | `rgba(247,242,231,0.6)` |
| 印泥红(印章/UI) | `#b03a2e` |
| 朱砂信子 | `#b03a2e` |
| 墨字(正文) | `#1a1a1a` 标题 / `#3a3a3a` 正文 |
| 未点亮灰 | `#9e9e9e` |
| 投影 | `rgba(60,50,30,0.18)` offset(2,2) blur 2 |

### 1. 纸(画布背景)
- 每层底色按上表 `z%3` 循环;**不要格线**(宣纸留白)
- 纤维纹理:离屏 canvas 一次性生成 3 张(每纸色一张):80 根随机短弧线(len 8–30px, alpha 0.5)+ 40 个随机小点(r 0.5–1, `#ece4d0`);每帧 `drawImage` 对应层纸,零性能成本

### 2. 墨蛇(核心,替代现有"节+胶囊"画法)
- **画法(给 opencode 的精确步骤)**:设 `n = onLayer.length`,对 `i = 1..n-1` 画线段:起点 = 第 i-1 节中心,终点 = 第 i 节中心,`lineCap='round'`
  - 第一遍(墨晕):`lineWidth = w(i)+4`,颜色墨晕色,画全部段
  - 第二遍(主墨):`lineWidth = w(i)`,颜色 = `lerp(#1a1a1a, #555, i/n)`,画全部段
  - 宽度函数:`w(i) = 13 - 8 * (i / n)`(头端 13 → 尾端 5,线性)
  - **穿墙跳变段**(首尾坐标不相邻)跳过不画(沿用现有相邻判断)
  - 飞白:对最后 30% 的段,沿段方向居中画 1–2 根 1px 飞白色短线(长约段长 60%)
- 演示蛇同画法但宽度 8→4、整体 alpha 0.3(沿用现有 !started 分支)
- **兼容**:blink/ghost 的 alpha 包裹套在最外层,墨晕/主墨/飞白全部在内

### 3. 毛笔头(替代圆头)
- 笔锋:水滴形——从 (cx,cy) 向移动方向收尖至 `cx+dir.x*11, cy+dir.y*11`,底宽 10px(两侧点 = 中心 ± 垂直方向 5px),填充浓墨 `#1a1a1a`
- 笔杆:从中心向**移动反方向**延伸 10px 的 5px 圆头线,双色分段(近端 `#8d6e63` 6px + 远端 `#a1887f` 4px),杆尾金箍 2px `#c9a227`(压在墨线上,像笔拖着墨迹)
- 眼:两颗白点(r 1.8)位于笔锋根部两侧(垂直 dir 偏移 ±3、前移 2)
- 吐信:朱砂色分叉信子从笔锋尖 (11px) 伸至 16px(沿用现有分叉几何,整体前移)
- 投影:仅头部开 `shadowColor=投影色 shadowOffset(2,2) shadowBlur=2`,画完即关(全身墨线不开投影——墨在纸上本无影,且保性能)

### 4. 砚台果子(`fillFruitShape` 重写,签名不变)
- 规格(size 缩放,主层 size=20,图鉴 size=14,虚影同主层):砚身椭圆 rx=0.4size, ry=0.325size,填充 `#3e4450`、描边 1px `#262b33`;砚池椭圆 rx=0.25size, ry=0.2size,填充**效果墨色**;池左上小高光弧白 alpha 0.25
- 效果墨色表(传统国画色,主色语义与现版一一对应):

| 类型 | 墨池色 |
| --- | --- |
| normal | 朱砂 `#b03a2e` |
| fast | 石绿 `#1e8449` |
| slow | 靛青 `#2874a6` |
| grow | 藤黄 `#d4ac0d` |
| shrink | 紫矿 `#7d3c98` |
| reverse | 赭石 `#ca6f1e` |
| up | 胭脂 `#c2185b` |
| down | 青碧 `#148f77` |
| gold | 泥金 `#b7950b` |
| teleport | 玄灰 `#566573` |
| clock | 月白 `#e8f0f0` |
| bomb | 池白 `#ffffff` + 砚身 `#8e2b25` + **外圈白描边 1.5px**(危险语义) |
| double | 泥金池 + **砚沿金边脉动保留**(现有 sin 逻辑) |
| ghost | 池空淡墨 `rgba(80,80,80,0.45)`("洗过的砚") |
| rainbow | **墨分五色**:墨池浓淡循环动画(`#1a1a1a`↔`#888888`,现有 t 驱动) |

- 投影:与头部同规格(果仅 3 个/层,开销可忽略);虚影里不开投影、不开高光(极简剪影即可)

### 5. 墙格 = 镇纸墨块(`drawWall` 重写,签名不变)
- 圆角 3px 方块(16×16 居中),填充 `#2f3640`、描边 1.5px `#1b2126`、石纹两道 `#454e5c`(45° 双斜线沿用);投影同上

### 6. 印章 UI(得分/层号/限时倒计时)
- 统一"朱文印":圆角 3 方块,底印泥红 `#b03a2e`,白字 sans-serif bold,整体旋转 -3°(ctx.rotate)
- 得分章(左上 10,10 尺寸约 52×24):上行 9px"得分"、下行 14px 数字
- 层号章(得分章右侧间距 6):上行"层"、下行数字
- 限时章(右上对称):"剩余" + m:ss;**≤10 秒整章闪烁**(alpha 0.4/1 交替)
- buff 倒计时(加速/减速/双倍/隐身):保留文字形式,色值换对应墨池色,前面加一个 r3 实心墨点
- 方向反转倒计时:文字色赭石

### 7. toast 与覆盖层
- toast:胶囊底 `rgba(38,40,40,0.78)` + 白字(测量文字宽定胶囊宽,内边距 10×5)
- 覆盖层(开始/暂停/结算/通关):底色 `rgba(247,242,231,0.88)`;中央**题跋卡片**:圆角 10、底 `#fbf7ee`、描边 1px `#c9bfa8`、投影同上;卡片内标题 22px 墨字、正文 14px `#3a3a3a`
- 通关/结算卡片右上加**旋转大印**(-8°,"通关"/"结算" 白字红底),破纪录时印章文字改"新纪录"
- 开始界面标题"贪吃蛇"22px 墨字,其下小字副题"笔墨纸砚·伪三层"10px `#9e9e9e`(可选)

### 8. 图鉴(暂停界面)
- 果子小样 = 砚台规格(size=14);文字墨字;未点亮 `#9e9e9e` + 小样 alpha 0.35(现有逻辑);双列布局保留

### 9. 实现注意与禁令
- 纸纹理必须离屏缓存 3 张;**禁止每帧生成纤维**
- lerpColor 写成小工具(墨蛇渐变/墨池均可复用);禁止每段 stroke 创建渐变对象
- 投影只允许出现在:毛笔头、砚台、墙块、题跋卡片;其余元素禁用 shadow*(性能)
- `fillFruitShape(type, px, py, size)`、`drawWall(x, y)` 签名不变;逻辑层零改动;roundRect rect fallback 保留
- 虚影(0.15)、无敌闪烁(0.25)、隐身(0.45)、图鉴未点亮(0.35)alpha 机制原样兼容

### 10. 验收清单(老板试玩)
1. 三种纸色轮换可感知;纤维质感淡而不脏
2. 墨蛇:一笔连贯、头粗尾细、有墨晕、尾段飞白;毛笔头方向感清晰(笔锋+笔杆+金箍)
3. 砚台果:池色 15 种可辨;炸弹白边、双倍金边脉动、彩虹"墨分五色"动画正确
4. 印章 UI 三处(得分/层号/倒计时)对齐无重叠;限时末 10 秒闪烁
5. 镇纸墙与墨蛇区分清晰
6. 题跋卡片 + 通关大印;图鉴双列砚台小样
7. 三模式、暂停/结算、虚影、无敌、隐身全部正常,60fps 无卡顿

## 【存档】第一轮路线 A:「翠玉丛林」——浅色底 + 翠玉渐变蛇 + 弹珠果子

> 已被第二轮「翠玉竹林」主题吸收升级;以下配色/弹珠规格仍可作为竹林主题的实现参考。

定位:雅致耐看、改动可控(逻辑零动、纯绘制),果子"弹珠化"是观感提升最大的一笔。

### A1 全局
- 页面背景(body):`#d8e0d0` 浅灰绿
- 画布底:`#f4f7ee` 暖白;**新增淡格线**:每格 1px `#e4e9dc`(先铺格线再画其他)
- 画布边框:2px `#2e7d32`(墨绿,替代纯黑)
- HUD 文字:得分/层号 `#33691e` 加粗;buff 倒计时沿用各自语义色;限时红色 `#d32f2f` 不变
- toast:圆角胶囊底 `rgba(38,50,56,0.72)` + 白字(替代现在的裸文字)
- 覆盖层(开始/暂停/结算/通关):底 `rgba(244,247,238,0.88)`;内容改画**圆角卡片**:白底、描边 2px `#2e7d32`、圆角 12px、轻阴影(`shadowColor rgba(0,0,0,0.18)` `shadowBlur 10`);标题墨绿、正文深灰 `#37474f`
- "贪吃蛇"标题字色 `#2e7d32`

### A2 蛇(核心改造)
- **身体渐变**:按节从头到尾在 `#2e7d32`(深)→ `#66bb6a`(中)→ `#a5d6a7`(浅)之间做 RGB 线性插值(写一个 lerpColor 小工具函数,缓存每节色字符串),头深尾浅,一眼蛇形
- 圆头:半径 9.5 不变,填充 `#1b5e20`(比身体更深),外加 1.5px `#0d3d12` 描边
- 眼睛:升级为**椭圆眼**——眼白 4×5px、黑瞳 2×2,位置逻辑沿用现有 dir 偏移
- 吐信:保留分叉造型,颜色 `#e53935`
- 尾部渐细:保留现有两节收窄(inset 5/6.5),颜色参与整体渐变
- 胶囊连接:连接件颜色取两节插值色的中间值,保证渐变连续
- 无敌闪烁/隐身半透明:alpha 包裹逻辑原样保留,只作用于最终颜色

### A3 果子(弹珠化,15 种全改)
- 通用规格:**圆形**半径 8.5(居中于格);主色填充 → 左上 45° 高光点(白 `rgba(255,255,255,0.75)`,半径 2.5,偏移 (-3,-3))→ 1.5px 同色系深色描边
- 主色与描边对照表(主色 = 现有 FRUIT_STYLES 色值不变,保证图鉴/记忆连续性):

| 类型 | 主色 | 描边 |
| --- | --- | --- |
| normal | #f00 | #a00 |
| fast | #0a0 | #070 |
| slow | #06c | #049 |
| grow | #c90 | #970 |
| shrink | #a0c | #80a |
| reverse | #f80 | #c60 |
| up | #f0f | #c0c |
| down | #0cc | #099 |
| rainbow | 动画渐变(现有) | #000 保留 |
| gold | #fd0 | #c8a000 |
| teleport | #666 | #444 |
| clock | #fff | #000 保留 |
| bomb | #a00 | **白边 2px 保留**(危险语义) |
| double | #ccc | 金边脉动保留 |
| ghost | rgba(120,120,120,0.5) | #000 保留 |

- `fillFruitShape()` 单点改造:方块 → 圆 + 高光 + 描边;主层/虚影/图鉴自动同步(图鉴色块同步弹珠化,半径 7)

### A4 墙格
- 圆角块:圆角 3px、底 `#cfd8dc`(蓝灰)、描边 1.5px `#78909c`、斜纹 `#b0bec5`(45° 双斜线保留)
- 与彩色弹珠果子、灰色 ghost 果(半透明小圆)的区分:墙是大块+斜纹+不透明,依旧一眼可辨

### A5 动画与细节
- 爬层动画/层号弹跳:文字色 `#2e7d32`,白闪改为 `rgba(244,247,238,k)` 暖白闪
- 相邻层果子虚影:弹珠画法在 globalAlpha 0.15 下照常工作,无需特判
- 图鉴:未点亮灰显文字 `#9e9e9e` 不变;点亮行文字 `#33691e`
- "新纪录!"用 `#d32f2f` 加粗

---

## 【存档】第一轮路线 B(备选概要):「暗夜霓虹」

> 已被第二轮「赛博 2077」主题吸收升级。

深底 `#0f141f` + 蛇霓虹青 `#00e5ff`(`shadowBlur` 发光)+ 果子霓虹光点 + 格线 `#1c2436`。游戏感最强,但所有对比度、虚影、图鉴、覆盖层全部重调,改动量约为 A 的两倍,且浅色系 UI 文字全部要反色。**如老板想要"电竞感"再选这条**,A 的结构规范同样适用,只是调色板替换。

---

## 实现验收清单(老板试玩看这些)
1. 蛇:翠玉渐变(头深尾浅)、圆头描边、椭圆眼、红信、渐细尾
2. 果子:15 种全部弹珠化,高光可见、颜色可辨、描边正确(特别是炸弹白边、双倍分金边脉动)
3. 墙格:蓝灰砖块与果子和 ghost 果区分清晰
4. 格线淡而不抢;toast 胶囊;覆盖层圆角卡片
5. 图鉴双列同步弹珠化;虚影/无敌闪烁/隐身半透明在新配色下依然可读
6. 三种模式 + 暂停/结算界面无错位、无文字重叠

## 版本与回滚
- opencode 单独一个 commit(下一个版本号以 git log 为准),README"基本设定/开发注意事项"中"白底黑蛇"表述同步更新为"翠玉丛林"新约定
- 纯绘制层改动,老板不满意可整体 revert 单个 commit,零机制风险
