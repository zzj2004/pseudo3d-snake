# 下一批开发方案(Zcode 版,与 PLAN.md 平行)

状态:**草稿,待老板审核**。本文档与 `PLAN.md`(opencode 版)平行,供老板对照两份方案后汇总定稿到 IDEAS.md。汇总前不动代码。
两份文档的共识部分本文档从简,重点写:**我的独立设计、比 PLAN.md 更细的技术方案、以及与 PLAN.md 的差异点**。

---

## 一、诊断(量化补充)

与 PLAN.md 共识一致(根因 = 无尽不死 + 果子永久生效),补充两个量化观察,说明"为什么刚好 5 分钟失控":

1. **速度公式拆解**:当前每步间隔 = `滑块(80–300) + speedOffset − score×5`。举例:滑块 150ms,吃 3 颗绿果(−90,永久),打到 40 分(−200),结果 = −140 → 触发 50ms 下限,即**每秒 13.3 格**,已超人类反应极限。绿果累积和分数加速两项叠加,5 分钟左右必然触底。
2. **爬层频率随蛇长失控**:蛇长 L 时每步撞身概率约 L/400(同层估算)。L=40 时约 10%/步(平均 10 步爬一层),L=80 时约 20%(平均 5 步一层)——爬层从"事件"变成"背景噪音",这就是"重复升层空转"的数学来源。

结论:①②是同一个病(参数只增不减),③是结构病(无失败条件)。三步走顺序我同意 PLAN.md:**果子限时改革 → 多模式 → 新果子+层深度收益**。

---

## 二、第 1 步:果子限时改革(我的具体设计)

| 项 | 设计 | 与 PLAN.md 的差异 |
| --- | --- | --- |
| 绿果(快) | 限时 **6 秒**,期间速度 −60ms,到时自动恢复,顶部显示剩余秒数 | PLAN.md 写 8 秒/未定量;6 秒与"橙果反转 8 秒"错开,同屏两个倒计时不混淆 |
| 蓝果(慢) | 限时 **6 秒**,期间速度 +60ms | 同上对称 |
| 得分加速 | `score×5` 改为 `min(100, score×5)`,即最多加速 100ms 封顶 | 与 PLAN.md 一致 |
| speedOffset 变量 | **退役**,新增加 fastUntil/slowUntil 时间戳(与 reverseUntil 同机制) | PLAN.md 未提实现 |
| 新增时钟果 | 清空当前所有速度效果(含未到时的快/慢 buff),手感立即回到"滑块+分数加速"基准;颜色建议白 `#fff`+黑描边(现有 11 色外最干净的空位) | PLAN.md 也有时钟果;我补充配色与"清 buff"精确定义 |

参数(6 秒/−60ms/上限 100)都可调,实现时集中放常量区,老板试玩后随时改。

---

## 三、第 2 步:三模式系统(我的完整设计,本步由我实施)

### 模式定义

| 模式 | 规则 | 目标人群 |
| --- | --- | --- |
| 无尽(默认) | 现状原样保留,零改动 | 休闲/怀旧 |
| 限时 | 倒计时 **180 秒**冲分,时间到结算 | 短平快刷分 |
| 有命 | 3 条命;吃炸弹果扣 1 命(方案 A);命尽结算 | 压力/张力 |

**与 PLAN.md 的差异:限时模式我建议 180 秒而非 120 秒**——理由:现有速度曲线下(得分加速封顶 100ms 后),2 分钟刚进入高速区就结束,3 分钟正好走完"从容→紧张→极限"完整体验;且 180 秒与"每 100 分 toast"的节奏匹配(高手一局约 150–250 分)。

### 有命模式细节(方案 A,我支持 A)

- 炸弹果**仅在有命模式出现**(无尽/限时不含,避免休闲局被劝退);随机池中占 1 份
- 外观:暗红 `#a00` + 白色边框(与普通红 `#f00` 区分,白边框强化"危险"语言,与墙格"白底黑框"镜像区分)
- 扣命后 **2 秒无敌闪烁**(蛇半透明闪烁),期间不判任何致死事件,防连扣
- 顶部显示"命 ×3";扣命时有专属音效(低沉双音)与画面轻震(可选)
- 命尽 → 结算界面

### 交互与界面设计(尽量少加新交互)

- 模式选择:**控制栏新增一个"模式:无尽"循环按钮**(点击在 无尽→有命→限时 间切换),不引入画布内点击热区——与现有"全按钮控制栏"风格一致,实现简单可靠
- 开始界面覆盖层显示当前模式说明一行(如"有命模式:3 条命,小心炸弹果")
- 限时模式顶部中央显示倒计时(与得分同行右侧);最后 10 秒数字变红
- **统一结算界面**(限时时间到 / 有命命尽):模式名、本局得分、该模式最高分(新纪录时标注"新纪录!")、"点击开始按钮再来一局"

### 存档 v2(本步一并做)

```
{ v: 2,
  best: { endless: 0, survival: 0, timed: 0 },   // 按模式分
  sound: false,
  stats: { climbs: 0, descents: 0, fruits: {} },  // 保持全局总战绩
  book: { ... } }
```

- 旧档迁移:读到 v1 自动升级为 v2,原 best 填入 `best.endless`,无感过渡
- 结算与开始界面按当前模式显示对应最高分

---

## 四、第 3 步:新果子批 + 层深度收益

- **层深度收益**:金果/彩虹果在随机池中的份数随层数增加(如每 3 层 +1 份,设上限)——爬层从"事故"变"追求";高层同时略微提高墙格数量上限,风险收益对冲(此项试玩后可单独关闭)
- 新果子候选(颜色均不与现有 11 种冲突):

| 果子 | 效果 | 建议颜色 | 挂哪个模式 |
| --- | --- | --- | --- |
| 炸弹 | 扣 1 命 | 暗红 `#a00`+白边 | 仅有命 |
| 双倍分 | 限时 10 秒得分 ×2 | 银白 `#ccc`+闪烁描边 | 全模式 |
| 隐身 | 5 秒内穿身不爬层 | 半透明灰(绘制时 alpha 0.5) | 有命价值最大,全模式可用 |
| 剪刀 | 蛇身减半(向下取整,最少 2 节) | 棕 `#860` | 全模式 |

- PLAN.md 候选中的"限时慢动作果"我建议**砍掉**——与第 1 步改造后的限时蓝果功能重叠
- 通关线 999999999 保留在无尽模式;限时/有命以结算界面为目的,通关概念不适用

---

## 五、实施与验收节奏

| 步骤 | 内容 | 认领 | 预计 |
| --- | --- | --- | --- |
| 1 | 果子限时改革 + 时钟果 + 加速上限 | opencode(PLAN.md 预认领,我同意) | 1 个版本 |
| 2 | 三模式 + 结算界面 + 存档 v2 | Zcode(我) | 1 个版本(本批最大) |
| 3 | 新果子批 + 层深度收益 | 届时再分 | 1 个版本 |

- 每步一个 commit、一个可试玩版本,老板**每步试玩通过再继续**
- 无尽模式行为除限时 buff 外零变化;每步独立提交,不满意可整体回滚
- 关卡制:二期大版本。技术预案先记 here:每关 = `{目标层数, 墙布局, 果子池, 初始速度}`,复用 layers[z] 结构,存档加 progress 字段升 v3——本次不做,仅防方案丢失

---

## 六、给老板的拍板清单(与 PLAN.md 汇总后)

1. 三步顺序 1→2→3:是/否
2. 限时 buff 参数:6 秒(我)还是 8 秒(PLAN.md)
3. 限时模式时长:180 秒(我)还是 120 秒(PLAN.md)
4. 有命模式:方案 A 炸弹果(两份文档都推荐)还是 B 撞身扣命
5. 第 3 步新果子取舍(砍"慢动作果"是否同意;四个候选要哪几个)
